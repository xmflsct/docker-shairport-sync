version: '3'
services:
  shairport-sync:
    restart: always
    network_mode: host
    devices:
      - '/dev/snd'
    environment:
      - AIRPLAY_NAME=pict
    volumes:
      - '/home/pi/dockers/shairport-sync/shairport-sync.conf:/etc/shairport-sync.conf'
    image: 'shairport-sync:latest'
